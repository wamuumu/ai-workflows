{
  "expected_tool_calls": {
    "atomic": {
      "analyze_sentiment": {"min": 1, "max": 3},
      "clean_text": {"min": 0, "max": 1},
      "translate_text": {"min": 0, "max": 1}
    },
    "macro": {},
    "common": {}
  },
  "expected_llm_calls": {
    "atomic": {"min": 2, "max": 6},
    "macro": {"min": 2, "max": 6}
  },
  "expected_branch_transitions": {
    "atomic": {
      "confidence_check": {
          "keywords": ["confidence", "low", "uncertain", "threshold", "weak", "insufficient", "retry", "reclassify"],
          "transitions": 2
      }
    },
    "macro": {
      "confidence_check": {
          "keywords": ["confidence", "low", "uncertain", "threshold", "weak", "insufficient", "retry", "reclassify"],
          "transitions": 2
      }
    }
  },
  "expected_step_count_range": {
    "atomic": {"min": 3, "max": 8},
    "macro": {"min": 3, "max": 8}
  }
}
