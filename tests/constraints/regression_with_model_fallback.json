{
  "expected_tool_calls": {
    "atomic": {
      "compute_statistics": {"min": 1, "max": 2},
      "train_regression": {"min": 1, "max": 1},
      "make_predictions": {"min": 1, "max": 1}
    },
    "macro": {},
    "common": {
      "send_email": {"min": 0, "max": 1}
    }
  },
  "expected_llm_calls": {
    "atomic": {"min": 4, "max": 8},
    "macro": {"min": 4, "max": 8}
  },
  "expected_branch_transitions": {
    "atomic": {
      "variance_check": {
          "keywords": ["variance", "threshold", "low", "below", "under", "check", "condition"],
          "transitions": 2
      }
    },
    "macro": {
      "variance_check": {
          "keywords": ["variance", "threshold", "low", "below", "under", "check", "condition"],
          "transitions": 2
      }
    }
  },
  "expected_step_count_range": {
    "atomic": {"min": 5, "max": 10},
    "macro": {"min": 5, "max": 10}
  }
}
