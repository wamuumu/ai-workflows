{
  "expected_tool_calls": {
    "atomic": {
      "get_news": {"min": 1, "max": 2},
      "get_stock_price": {"min": 1, "max": 2},
      "calculator": {"min": 0, "max": 2}
    },
    "macro": {},
    "common": {
      "send_email": {"min": 0, "max": 1}
    }
  },
  "expected_llm_calls": {
    "atomic": {"min": 3, "max": 7},
    "macro": {"min": 3, "max": 7}
  },
  "expected_branch_transitions": {
    "atomic": {
      "stock_direction_check": {
          "keywords": ["up", "down", "increased", "decreased", "higher", "lower", "rose", "fell", "positive", "negative"],
          "transitions": 2
      }
    },
    "macro": {
      "stock_direction_check": {
          "keywords": ["up", "down", "increased", "decreased", "higher", "lower", "rose", "fell", "positive", "negative"],
          "transitions": 2
      }
    }
  },
  "expected_step_count_range": {
    "atomic": {"min": 5, "max": 10},
    "macro": {"min": 5, "max": 10}
  }
}
